import Cluster.kmeans
import Data.GeneratedData as data
from Data.MNIST import MNIST

# gd = data.GeneratedData()
# cluster = Cluster.kmeans.Cluster(gd.point_set)
# cluster.show()  # 测试通过

# mnist = MNIST()
# mnist.load_data()
# cluster = Cluster.kmeans.Cluster(mnist.point_set)
# print(cluster.centroids.shape)
# print(cluster.labels)  # Mnist加载测试通过
